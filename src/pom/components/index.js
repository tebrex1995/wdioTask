const ErrorComponent = require('./common/error.component');
const Headers = require('./common/headers.component');

const LoginComponent = require('./login.component');

module.exports = {
  Headers,
  ErrorComponent,
  LoginComponent,
};
